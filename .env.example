# Driplo Production Environment Variables Template
# Copy this file to .env.local for local development
# Configure these in Vercel for production deployment

# =============================================================================
# REQUIRED FOR PRODUCTION
# =============================================================================

# Supabase Configuration (Required)
PUBLIC_SUPABASE_URL=https://koowfhsaqmarfdkwsfiz.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Stripe Payment Processing (Required for payments)
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key_here
STRIPE_SECRET_KEY=sk_live_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Security Secrets (Required for production)
RATE_LIMIT_SECRET=generate_32_character_random_string_here
CSRF_SECRET=generate_32_character_random_string_here

# Environment Configuration
NODE_ENV=production
VERCEL_ENV=production

# =============================================================================
# HIGHLY RECOMMENDED FOR PRODUCTION
# =============================================================================

# Error Monitoring (Sentry)
PUBLIC_SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id
SENTRY_AUTH_TOKEN=your_sentry_auth_token
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=driplo-web

# Application URLs
PUBLIC_APP_URL=https://driplo.xyz
PUBLIC_SITE_URL=https://driplo.xyz

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================

# Email Service (Resend)
RESEND_API_KEY=re_your_resend_api_key_here

# Analytics
GOOGLE_ANALYTICS_ID=G-your_ga_id_here
MIXPANEL_TOKEN=your_mixpanel_token_here

# Cloud Storage (if using)
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
AWS_S3_BUCKET=your_bucket_name
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# =============================================================================
# DEVELOPMENT ONLY (DO NOT SET IN PRODUCTION)
# =============================================================================

# Only use these for local development
# VITE_LOG_LEVEL=debug
# DATABASE_URL=postgresql://localhost:5432/driplo_dev

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEVER commit real secrets to version control
# 2. Use Vercel environment variables for production
# 3. Generate strong random strings for secrets:
#    - Use: openssl rand -hex 32
#    - Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Rotate secrets regularly in production
# 5. Use different keys for staging and production

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local for local development
# 2. Replace all placeholder values with real credentials
# 3. For production deployment:
#    - Add these variables in Vercel dashboard
#    - Never include .env files in deployment
#    - Use Vercel CLI: vercel env add VARIABLE_NAME

# =============================================================================
# VALIDATION
# =============================================================================

# After setting up environment variables, validate with:
# npm run build
# npm run check-types
# npm run lint
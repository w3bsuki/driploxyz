/**
 * Domain Adapters
 *
 * These adapters bridge between the domain layer and the application layer.
 * They maintain backward compatibility while leveraging domain services.
 */

// Product domain adapter
export * from './products.domain.js';

// Category domain adapter
export * from './category.domain.js';